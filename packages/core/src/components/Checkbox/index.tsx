export { default as Checkbox } from './Checkbox'

{
  /* <Checkbox
    defaultChecked
    color="red"
    leftLabel="Apple"
    value="apple"
    onChange={(value, checked) => alert(value + '-' + checked)}
/> */
}
